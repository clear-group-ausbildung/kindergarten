# -------------------------------------------------------------
# DEFAULT INVOICING BUILD PROPERTIES
# -------------------------------------------------------------
#
# DO NOT EDIT THIS FILE IN ORDER TO CUSTOMIZE BUILD PROPERTIES.
# CREATE AND EDIT build.properties FILE INSTEAD.
#

# Project Properties ------------------------------------------
Name=Kassenverwaltung
Shortname=Kassenverwaltung
name=kassenverwaltung
spec.version=1.0.0
impl.version=1.0.0
version.name=${impl.version}
dist.version=1_0_0-2017-08-23
main.class=de.clearit.kindergarten.application.KindergartenLauncher
vendor=Clear Group
copyright.date=2017
copyright.owner=${vendor}
copyright.message=Copyright &#169; ${copyright.date} ${copyright.owner}. All Rights Reserved.
copyright.short=(c) 2017 Clear Group 

top.dir=${basedir}


# Source Properties -------------------------------------------
src.dir          = ${top.dir}/src
src.java.dir     = ${src.dir}/main/java
docs.dir         = ${top.dir}/docs


# Library Properties ------------------------------------------
lib.dir = ${top.dir}/lib
lib.apple.jar      = ${lib.dir}/AppleJavaExtensions-1.4.jar
lib.application.jar= ${lib.dir}/application-0.9.12.jar
lib.binding.jar    = ${lib.dir}/binding-2.0.6.jar
lib.completion.jar = ${lib.dir}/completion-1.1.2.jar
lib.desktop.jar    = ${lib.dir}/desktop-0.9.16.jar
lib.forms.jar      = ${lib.dir}/forms-1.3.0pre4.jar
lib.help.jar       = ${lib.dir}/help-0.9.23.jar
lib.jsdl-core.jar  = ${lib.dir}/jsdl-core-0.9.10.jar
lib.looks.jar      = ${lib.dir}/looks-2.2.2.jar
lib.quicksearch.jar= ${lib.dir}/quicksearch-0.7.8.jar
lib.uif2.jar       = ${lib.dir}/uif2-0.9.18.jar
lib.validation.jar = ${lib.dir}/validation-2.0.1.jar
lib.worker.jar     = ${lib.dir}/swing-worker.jar


# JavaDoc Properties ------------------------------------------
javadoc.link=http://docs.oracle.com/javase/8/docs/api/
javadoc.packages=de.clearit.kindergarten.*


# Build Properties --------------------------------------------
build.compiler.pedantic=false
build.compile.debug=on
build.compile.deprecation=off
build.compile.fork=no
build.compile.nowarn=on
build.compile.source=1.8
build.compile.target=1.8
build.encoding=ISO-8859-1

build.dir          = ${top.dir}/build
build.classes.dir  = ${build.dir}/classes
build.main.dir     = ${build.classes.dir}/main
build.docs.dir     = ${build.dir}/docs
build.javadocs.dir = ${build.docs.dir}/api
build.reports.dir  = ${build.dir}/test-reports

build.manifest     = ${build.dir}/manifest.mf
build.main.jar     = ${build.dir}/${name}.jar
build.shrinked-out.jar=${build.dir}/shrinked/${name}.jar
build.shrinked.jar = ${build.dir}/${name}-shrinked.jar
build.signed.jar   = ${build.dir}/${name}-signed.jar
build.shrinked.compressed.jar = ${build.dir}/${name}-shrinked-compressed.jar
build.signed.compressed.jar   = ${build.dir}/${name}-signed-compressed.jar

# These properties are required for the shrinking and signing
#shrink.boot.class.path=
#sign.keystore  = 
#sign.alias     = 
#sign.storepass =

# These properties are required for the NSIS installer
#nsis.dir       = /JGoodies/NSIS
#nsis.exe       = /Programme/NSIS/makensis.exe
#nsis.include
nsis.script    = ${build.dir}/${name}.nsi


# Dist Properties -----------------------------------------------
dist.root.dir        = ${top.dir}/dist
dist.name     	     = ${name}-${impl.version}
dist.name.mac        = ${Shortname} ${spec.version}
deploy.name          = ${name}-${dist.version}

dist.source.subdir   = ${dist.name}-src
dist.source.dir      = ${dist.root.dir}/${dist.source.subdir}
dist.source.docs.dir = ${dist.source.dir}/docs
dist.source.lib.dir  = ${dist.source.dir}/lib
dist.source.src.dir  = ${dist.source.dir}/src
dist.source.main.jar = ${dist.source.dir}/${dist.name}.jar
dist.source.zip      = ${dist.root.dir}/${deploy.name}-src.zip

dist.binary.subdir   = ${dist.name}
dist.binary.dir      = ${dist.root.dir}/${dist.binary.subdir}
dist.binary.main.jar = ${dist.binary.dir}/${dist.name}.jar
dist.binary.zip      = ${dist.root.dir}/${deploy.name}.zip

dist.binary.subdir.mac=${dist.name.mac}
dist.binary.dir.mac  = ${dist.root.dir}/${dist.binary.subdir.mac}
dist.binary.zip.mac  = ${dist.root.dir}/${deploy.name}-mac.zip

dist.jnlp.subdir     = ${dist.name}-jnlp
dist.jnlp.dir        = ${dist.root.dir}/${dist.jnlp.subdir}
dist.jnlp.main.jar   = ${dist.jnlp.dir}/${name}.jar

dist.binary.win.name = ${deploy.name}-win.exe
